# simple-http-server
A lightweight, multi-threaded C++ HTTP server built from scratch using modern C++17. It handles basic GET requests, serves static files, manages concurrency with a custom thread pool, and demonstrates low-level socket programming, RAII, and clean object-oriented design.
